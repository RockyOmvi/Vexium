Parsing: examples\test_lexer.vex

=== AST ===
Program(41 stmts)
  Let(count)
    Int(0)
  Let(name)
    String("Alice")
  Const(PI)
    Float(3.14159)
  Const(MAX)
    Int(1024)
  Let(hex_val)
    Int(255)
  Let(bin_val)
    Int(10)
  Let(big_num)
    Int(1000000)
  Let(sci)
    Float(1.5e+010)
  Fn(greet -> str)
    params: (person: str)
    body:
      Block(1 stmts)
        GiveBack
          String("Hello, {person}!")
  Fn(add -> int)
    params: (a: int, b: int)
    body:
      Block(1 stmts)
        GiveBack
          BinaryOp(PLUS)
            Id(a)
            Id(b)
  If
    cond:
      BinaryOp(IS)
        Id(count)
        Int(0)
    then:
      Block(1 stmts)
        Display
          String("Count is zero")
    else:
      If
        cond:
          BinaryOp(IS_GREATER)
            Id(count)
            Int(10)
        then:
          Block(1 stmts)
            Display
              String("Count is big")
        else:
          If
            cond:
              BinaryOp(IS_LESS)
                Id(count)
                Int(0)
            then:
              Block(1 stmts)
                Display
                  String("Negative!")
            else:
              Block(1 stmts)
                Display
                  String("Something else")
  While
    cond:
      BinaryOp(IS_LESS)
        Id(count)
        Int(10)
    body:
      Block(1 stmts)
        ExprStmt
          Assign(BE)
            Id(count)
            BinaryOp(PLUS)
              Id(count)
              Int(1)
  ForRange(i)
    start:
      Int(1)
    end:
      Int(20)
    step:
      Int(2)
    body:
      Block(1 stmts)
        Display
          Id(i)
  ForEach(item)
    iterable:
      Array(2 items)
        String("apple")
        String("banana")
    body:
      Block(1 stmts)
        Display
          Id(item)
  Repeat
    count:
      Int(5)
    body:
      Block(1 stmts)
        Display
          String("Hello!")
  Match
    Id(count)
    arm:
      Int(0)
      Display
        String("zero")
    arm:
      Int(1)
      Display
        String("one")
  Let(x)
    BinaryOp(PLUS)
      Int(10)
      Int(5)
  Let(y)
    BinaryOp(STAR)
      Id(x)
      Int(2)
  Let(z)
    BinaryOp(SLASH)
      Id(y)
      Int(3)
  Let(w)
    BinaryOp(PERCENT)
      Id(z)
      Int(4)
  Let(p)
    BinaryOp(POWER)
      Int(2)
      Int(8)
  If
    cond:
      BinaryOp(EQ)
        Id(x)
        Int(15)
    then:
      Block(1 stmts)
        Pass
  If
    cond:
      BinaryOp(NEQ)
        Id(x)
        Int(20)
    then:
      Block(1 stmts)
        Pass
  If
    cond:
      BinaryOp(GTE)
        Id(x)
        Int(10)
    then:
      Block(1 stmts)
        Pass
  If
    cond:
      BinaryOp(LTE)
        Id(x)
        Int(20)
    then:
      Block(1 stmts)
        Pass
  If
    cond:
      BinaryOp(IS)
        Id(x)
        Int(15)
    then:
      Block(1 stmts)
        Display
          String("fifteen")
  If
    cond:
      BinaryOp(IS_NOT)
        Id(x)
        Int(20)
    then:
      Block(1 stmts)
        Display
          String("not twenty")
  If
    cond:
      BinaryOp(IS_AT_LEAST)
        Id(x)
        Int(10)
    then:
      Block(1 stmts)
        Display
          String("at least ten")
  If
    cond:
      BinaryOp(IS_AT_MOST)
        Id(x)
        Int(100)
    then:
      Block(1 stmts)
        Display
          String("at most hundred")
  If
    cond:
      BinaryOp(AND)
        BinaryOp(GT)
          Id(x)
          Int(0)
        BinaryOp(GT)
          Id(y)
          Int(0)
    then:
      Block(1 stmts)
        Display
          String("both positive")
  If
    cond:
      BinaryOp(OR)
        BinaryOp(GT)
          Id(x)
          Int(100)
        BinaryOp(GT)
          Id(y)
          Int(100)
    then:
      Block(1 stmts)
        Display
          String("one is big")
  If
    cond:
      UnaryOp(NOT)
        Bool(false)
    then:
      Block(1 stmts)
        Display
          String("true!")
  Struct(Dog)
    has name: str
    has age: int
    can bark(self)
      Block(1 stmts)
        Display
          String("Woof!")
  Let(fruits)
    Array(2 items)
      String("apple")
      String("banana")
  Let(ages)
    Map(2 entries)
      key:
        String("alice")
      val:
        Int(30)
      key:
        String("bob")
      val:
        Int(25)
  Attempt
    try:
      Block(1 stmts)
        Display
          String("trying")
    otherwise(err):
      Block(1 stmts)
        Display
          String("failed")
  Use(math)
  FromUse(math.sqrt)
  Let(maybe)
    Nothing
  If
    cond:
      BinaryOp(IS)
        Id(maybe)
        Nothing
    then:
      Block(1 stmts)
        Display
          String("nothing there")
  Display
    String("All tokens tested!")
===========

