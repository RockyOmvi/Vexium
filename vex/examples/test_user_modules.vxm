from json use parse
from json use stringify
from math use sqrt

display "=== USER MODULES COMPAT TEST ==="

fn square(x):
	give back x * x

let payload be [square(2), square(3), square(4)]
display payload
display stringify(payload)
display sqrt(81)

let decoded be parse("[4,9,16]")
display decoded[0]
display decoded[2]
display "USER MODULES COMPAT PASS"
