from json use parse
from json use stringify
from math use sqrt

display "=== V2 COMPLETE COMPAT TEST ==="

fn square(n):
	give back n * n

let series be [square(1), square(2), square(3), square(4)]
display series
display stringify(series)

let parsed be parse("[10,20,30]")
display parsed[1]
display sqrt(49)

if len(series) == 4:
	display "series-ok"
else:
	display "series-bad"

display "V2 COMPLETE COMPAT PASS"
