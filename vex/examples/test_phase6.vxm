# VM-safe Phase 6 smoke test
display "=== 1. Lambda Functions ==="
let add be (a, b) => a + b
display add(3, 4)

fn power(base, exp):
    let result be 1
    for i in 0 to exp:
        result be result * base
    give back result

display "=== 2. Functions ==="
display power(5, 2)

let nums be [1, 2, 3, 4, 5]
let doubled be [x * 2 for each x in nums]
display "=== 3. Comprehensions ==="
display doubled

display "=== ALL PHASE 6 TESTS PASSED ==="
