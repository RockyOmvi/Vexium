display "=== COMPREHENSIVE COMPAT TEST ==="

let x be 10
let y be 3
display x + y
display x - y
display x * y

if x > y:
	display "x>y"
else:
	display "x<=y"

let total be 0
for i in 1 to 6:
	total be total + i
display total

fn fib(n):
	if n < 2:
		give back n
	give back fib(n - 1) + fib(n - 2)

display fib(8)

let items be ["a", "b"]
push(items, "c")
display len(items)
display items[2]

let cfg be {"name": "vex", "ver": 2}
display cfg
display "COMPREHENSIVE COMPAT PASS"
