from json use parse
from json use stringify
from math use sqrt

display "=== MODULES COMPREHENSIVE COMPAT TEST ==="

let values be [1, 4, 9]
display stringify(values)

let decoded be parse("[5,6,7]")
display decoded[1]

display sqrt(81)
display "MODULES COMPREHENSIVE COMPAT PASS"
